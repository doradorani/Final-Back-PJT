<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.office.agijagi_back.Util.Kakao.IKakaoMapper">
    <select id="isUser" parameterType="String" resultType="int">
        SELECT
            count(*)
        FROM
            userInfo
        WHERE
            email = #{email}
    </select>

    <insert id="newUser" parameterType="com.office.agijagi_back.Util.Kakao.KakaoTokenDto">
        INSERT INTO
            userInfo (userName, email, date)
        VALUES (
                   #{userName}, #{email}, NOW()
               )
    </insert>

</mapper>